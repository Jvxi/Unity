package com.jvxi.unity.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebConfig {
    // CORS is now handled by SecurityConfig to avoid duplicate headers
}